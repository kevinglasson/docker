apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-data-claim
spec:
  storageClassName: "efs-storageclass"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi